# eq-archives-software

The search frontend is deployed automatically from `master` to the k3s cluster
on eqvm. See the [deployment guide](elastic-indexer-stack/k8s-manifests/README.md)
for the workflow, production manifests, required Actions secrets, and rollback
instructions.
