[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=eqarchives_mcp --cov-branch --cov-report=term-missing --cov-fail-under=90"

[tool.coverage.run]
branch = true
source = ["eqarchives_mcp"]
