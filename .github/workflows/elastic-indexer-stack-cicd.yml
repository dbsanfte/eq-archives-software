name: Frontend CI/CD

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Test and build frontend
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    outputs:
      digest: ${{ steps.publish.outputs.digest }}
      mcp_digest: ${{ steps.publish-mcp.outputs.digest }}
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
      - name: Validate deployment scripts
        run: |
          bash -n scripts/deploy-frontend.sh scripts/smoke-frontend.sh scripts/smoke-embeddings.sh
          sh -n elastic-indexer-stack/k8s-manifests/embeddings/download-model.sh
          sh -n elastic-indexer-stack/k8s-manifests/embeddings/start-server.sh
          sh -n elastic-indexer-stack/k8s-manifests/embeddings/check-gpu.sh
          python3 -m py_compile scripts/frontend-secrets.py scripts/check-embeddings.py scripts/test-frontend-browser.py scripts/check-mcp.py scripts/mcp-smoke-fixture.py
          kubectl kustomize elastic-indexer-stack/k8s-manifests >/dev/null
      - uses: docker/setup-buildx-action@f87e5991a6d7451dcb8d9637bfbc97413f497069 # v4
      - name: Log in to Docker Hub
        if: github.ref == 'refs/heads/master' && github.event_name != 'pull_request'
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Reuse an image already published for this commit
        id: existing
        if: github.ref == 'refs/heads/master' && github.event_name != 'pull_request'
        env:
          IMAGE_TAG: dbsanfte/frontend:${{ github.sha }}
        run: |
          if docker pull "$IMAGE_TAG" > "$RUNNER_TEMP/frontend-pull.log" 2>&1; then
            echo 'reuse=true' >> "$GITHUB_OUTPUT"
          elif grep -Eq 'manifest unknown|manifest .* not found' "$RUNNER_TEMP/frontend-pull.log"; then
            echo 'No image has been published for this commit yet.'
          else
            cat "$RUNNER_TEMP/frontend-pull.log"
            exit 1
          fi
      - name: Test and build container
        if: steps.existing.outputs.reuse != 'true'
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc # v7
        with:
          context: elastic-indexer-stack/frontend
          load: true
          tags: dbsanfte/frontend:${{ github.sha }}
          build-args: |
            GIT_SHA=${{ github.sha }}
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
          cache-from: type=gha,scope=frontend
          cache-to: type=gha,mode=max,scope=frontend
      - name: Install browser regression test dependencies
        run: |
          python3 -m venv "$RUNNER_TEMP/frontend-browser"
          "$RUNNER_TEMP/frontend-browser/bin/pip" install -r scripts/browser-test-requirements.txt
          "$RUNNER_TEMP/frontend-browser/bin/playwright" install --with-deps chromium
      - name: Reuse the MCP image already published for this commit
        id: existing-mcp
        if: github.ref == 'refs/heads/master' && github.event_name != 'pull_request'
        env:
          IMAGE_TAG: dbsanfte/frontend:mcp-${{ github.sha }}
        run: |
          if docker pull "$IMAGE_TAG" > "$RUNNER_TEMP/mcp-pull.log" 2>&1; then
            echo 'reuse=true' >> "$GITHUB_OUTPUT"
          elif grep -Eq 'manifest unknown|manifest .* not found' "$RUNNER_TEMP/mcp-pull.log"; then
            echo 'No MCP image has been published for this commit yet.'
          else
            cat "$RUNNER_TEMP/mcp-pull.log"
            exit 1
          fi
      - name: Test and build MCP service with coverage
        if: steps.existing-mcp.outputs.reuse != 'true'
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc # v7
        with:
          context: elastic-indexer-stack/mcp
          load: true
          tags: dbsanfte/frontend:mcp-${{ github.sha }}
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
          cache-from: type=gha,scope=mcp
          cache-to: type=gha,mode=max,scope=mcp
      - name: Smoke and browser regression test the built image
        env:
          IMAGE_TAG: dbsanfte/frontend:${{ github.sha }}
          EXPECTED_GIT_SHA: ${{ github.sha }}
          BROWSER_TEST_PYTHON: ${{ runner.temp }}/frontend-browser/bin/python
        run: bash scripts/smoke-frontend.sh "$IMAGE_TAG"
      - name: Test the Nomic service through the frontend proxy
        env:
          IMAGE_TAG: dbsanfte/frontend:${{ github.sha }}
          MCP_IMAGE_TAG: dbsanfte/frontend:mcp-${{ github.sha }}
        run: bash scripts/smoke-embeddings.sh "$IMAGE_TAG" "$MCP_IMAGE_TAG"
      - name: Publish the tested image
        id: publish
        if: github.ref == 'refs/heads/master' && github.event_name != 'pull_request'
        env:
          IMAGE_TAG: dbsanfte/frontend:${{ github.sha }}
          REUSE_IMAGE: ${{ steps.existing.outputs.reuse }}
        run: |
          if [[ "$REUSE_IMAGE" != 'true' ]]; then docker push "$IMAGE_TAG"; fi
          image=$(docker image inspect --format '{{index .RepoDigests 0}}' "$IMAGE_TAG")
          # A full image name contains DOCKERHUB_USERNAME and GitHub drops it
          # as a masked job output. Pass only the non-secret digest.
          echo "digest=${image#*@}" >> "$GITHUB_OUTPUT"
      - name: Publish the tested MCP image
        id: publish-mcp
        if: github.ref == 'refs/heads/master' && github.event_name != 'pull_request'
        env:
          IMAGE_TAG: dbsanfte/frontend:mcp-${{ github.sha }}
          REUSE_IMAGE: ${{ steps.existing-mcp.outputs.reuse }}
        run: |
          if [[ "$REUSE_IMAGE" != 'true' ]]; then docker push "$IMAGE_TAG"; fi
          image=$(docker image inspect --format '{{index .RepoDigests 0}}' "$IMAGE_TAG")
          echo "digest=${image#*@}" >> "$GITHUB_OUTPUT"

  deploy:
    name: Deploy frontend to eqvm
    needs: build
    if: github.ref == 'refs/heads/master' && github.event_name != 'pull_request'
    runs-on: [self-hosted, Linux, X64, eqvm]
    timeout-minutes: 25
    concurrency:
      group: eqarchives-frontend-production
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
      - name: Check this is still the current master commit
        id: current
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          head=$(gh api "repos/$GITHUB_REPOSITORY/git/ref/heads/master" --jq '.object.sha')
          if [[ "$head" == "$GITHUB_SHA" ]]; then
            echo 'deploy=true' >> "$GITHUB_OUTPUT"
          else
            echo 'Skipping deployment: a newer commit is already on master.'
          fi
      - name: Apply manifests and verify the rollout
        if: steps.current.outputs.deploy == 'true'
        env:
          IMAGE: dbsanfte/frontend@${{ needs.build.outputs.digest }}
          MCP_IMAGE: dbsanfte/frontend@${{ needs.build.outputs.mcp_digest }}
          FRONTEND_ES_USERNAME: ${{ secrets.FRONTEND_ES_USERNAME }}
          FRONTEND_ES_PASSWORD: ${{ secrets.FRONTEND_ES_PASSWORD }}
          FRONTEND_OPENAI_API_KEY: ${{ secrets.FRONTEND_OPENAI_API_KEY }}
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          bash scripts/deploy-frontend.sh "$IMAGE" "$MCP_IMAGE"
          echo "Deployed \`$IMAGE\` to https://search.eqarchives.org" >> "$GITHUB_STEP_SUMMARY"
          echo "Deployed MCP \`$MCP_IMAGE\` to https://search.eqarchives.org/mcp" >> "$GITHUB_STEP_SUMMARY"
